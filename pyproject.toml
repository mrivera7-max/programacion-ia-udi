[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM"]
# N818: se conserva el prefijo "Error" propio de la nomenclatura en español.
ignore = ["N818"]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
